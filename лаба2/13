a, b, c, d = [int(input()) for i in range(4)] # Ввод чисел
print('', end='\t') # Вывод пустой строкии табуляция
for j in range(c, d + 1): # Объявляем цикл
    print(j, end='\t') # Выводим числа для умножения
print() # Выводим пустую строку
for i in range(a, b + 1): # Объявляем цикл
    print(i, end='\t') # Выводим числа для умножения
    for j in range(c, d + 1): # Объявляем цикл
        print(i * j, end='\t') # Выводим произведение
    print() # Выводим пустую строку
